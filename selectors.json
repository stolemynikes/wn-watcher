{
  "_README": [
    "Detection lives here, not in code, because this is the fragile part: it",
    "depends on Whatnot's wording and markup, which they change without",
    "telling anyone. When detection breaks, run `python watch.py probe` on an",
    "open stream, look at the text it dumps, and fix a regex here. No code",
    "change, no redeploy.",
    "",
    "Everything is a case-insensitive regex matched against the tab's visible",
    "text. A capture group, where present, is the value extracted."
  ],
  "live": {
    "_note": "Confirmed against real screenshots 2026-08-12. The banner sits top-right and reads 'Giveaway with N entries'; expanded, the next line is the prize and the line after is either 'Not eligible' or 'Open Mobile App To Enter'. Collapsed, only the first line is on screen — so prize and eligibility are unknown, and an unknown giveaway is still announced.",
    "giveaway_present": "Giveaway with [\\d.,\\s  ]*\\d\\s*k? entr",
    "entries": "Giveaway with ([\\d.,\\s  ]*\\d\\s*k?) entr",
    "prize": "Giveaway with [\\d.,\\s  ]*\\d\\s*k? entries?\\s*\\n+\\s*(?!Not eligible|Open Mobile App|Terms|Chat\\b|Watching)(.{2,90}?)\\s*(?:\\n|$)",
    "seller": "^@?([A-Za-z0-9_.-]{3,30})\\s*$",
    "_note_challenge": "Matched the same way the radar does it. A challenged tab is skipped, never reloaded.",
    "challenge": "just a moment|attention required|checking your browser|verify you are human|enable javascript and cookies to continue",
    "_note_counts": "The count is not always a bare integer: 1,234 in English, 1.234 in Dutch, a thin space, or 12k when the UI shortens it. The old \\d+ failed to match those outright, which hid the whole giveaway — and the biggest giveaways are exactly the ones with formatted numbers.",
    "_note_prize": "The line directly under the entry count. The lookahead stops it grabbing the eligibility line, the T&C link or the chat header when the banner is collapsed and there is no prize line at all.",
    "_note_eligibility": "The page says outright whether you can enter, which beats inferring it. 'Not eligible' is a buyers giveaway you have not bought into. 'Open Mobile App To Enter' means you can. Neither present = collapsed banner, so we do not know — and we notify, because a missed giveaway costs more than a redundant buzz.",
    "not_eligible": "Not eligible",
    "enterable": "Open Mobile App To Enter"
  },
  "purchases": {
    "_note": "SPEC open question 2. Rows are read with a CSS selector because innerText alone gives no row boundaries. The default is a guess; run `python watch.py probe` with the purchases tab open and send the output if it finds nothing.",
    "url_match": "activityTab=purchases",
    "row_selector": "[data-testid*='purchase'], [href*='/transaction/'], li",
    "_note_row_min": "Rows shorter than this are chrome/navigation, not purchases.",
    "row_min_length": 12,
    "win_hint": "giveaway|won|free"
  }
}
